{
  "package": "dag",
  "owner": "SP-07",
  "fixtures": [
    { "name": "node_line", "kind": "format", "state": "frozen", "input": "", "want": "want/node_line.jsonl", "note": "dag/deps.jsonl node line; type discriminator distinguishes node from edge" },
    { "name": "edge_line", "kind": "format", "state": "frozen", "input": "", "want": "want/edge_line.jsonl", "note": "dag/deps.jsonl edge line" },
    { "name": "nodeid", "kind": "format", "state": "frozen", "input": "", "want": "nodeid.json", "note": "the hand-derived NodeID table: every constructor's inputs and the exact spelling it must produce, written from SP-07 D-2 rather than captured from the implementation. internal/dag's TestNodeIDConstructors checks the constructors against it; a NodeID is the join key every later wave records under" },
    { "name": "graph_basic", "kind": "format", "state": "frozen", "input": "", "want": "graph-basic.jsonl", "note": "the dag/deps.jsonl worked example: a generation header, twelve nodes covering all nine kinds, ten edges covering all eight, one tombstone, at a fixed clock. internal/dag's TestGraphBasicGolden reproduces it byte for byte through Flush; SP-08, SP-09 and SP-12 assert against these bytes" },
    { "name": "crossing", "kind": "format", "state": "frozen", "input": "", "want": "crossing.json", "note": "segment_coupling(p) over graph_basic at twelve cut positions — 0, 1, every node position, and one past the end. internal/dag's TestCrossingEdgesGolden reproduces it and anchors both ends by hand" },
    { "name": "slice_backward", "kind": "format", "state": "frozen", "input": "", "want": "slice-backward.json", "note": "BackwardSlice over graph_basic for Thin=true and Thin=false: criteria, visit order and scores to five decimals. internal/dag's TestSliceGoldenBackward reproduces it and asserts the properties it pins — thin never reaches more than full, a criterion scores exactly 1" },
    { "name": "thin_vs_full", "kind": "format", "state": "frozen", "input": "", "want": "thin-vs-full.json", "note": "thin against full slicing over eight seeded synthetic graphs: per-seed sizes, size ratio, recall and precision, plus the means. internal/dag's TestThinVsFullComparison checks each recorded ratio within 0.02; no wall-clock timing is recorded, because a golden that differs every run pins nothing" }
  ]
}
