{
  "package": "config",
  "owner": "SP-01",
  "fixtures": [
    { "name": "appendix_c_defaults", "kind": "format", "state": "frozen", "input": "", "want": "../../config/appendix-c.jsonc", "note": "config.Defaults() marshalled, minus the runtime key, must deep-equal this byte-for-byte (§4.1, Appendix C). The bytes stay at testdata/golden/config/ because five plan documents cite that path; this manifest declares them, it does not relocate them. JSONC, not JSON — comments must be stripped before parsing." }
  ]
}
