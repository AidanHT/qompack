# Notes

This file exists so `TestExtract_None` has a real `.md` input. Nothing in it is a declaration,
even though it mentions `func Load()` and `class Parser {` in prose and in the fence below.

```go
func NotASymbol() {
	return
}
```
