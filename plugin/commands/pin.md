---
description: Pin an invariant so it is never summarized away
argument-hint: "<text>"
allowed-tools: Bash(qompack pin:*)
---

Run the Qompack pin command and report its output verbatim to the user.

!`qompack pin $ARGUMENTS`
