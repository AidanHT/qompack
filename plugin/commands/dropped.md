---
description: Report what the last compaction dropped and how to get it back
argument-hint: "[--json]"
allowed-tools: Bash(qompack dropped:*)
---

Run the Qompack dropped command and report its output verbatim to the user.

!`qompack dropped $ARGUMENTS`
