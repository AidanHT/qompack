# V3-SP-08 preflight: Interface-contract verification

Scope: `plans/V3-SP-08-observer-l0.md` lines 280–640 — the "Interface contract" (Consumes /
Produces) block and the "Pre-step: the `arch/sp08-observer-seams` amendment" section.
Every claim was checked against the tree at `C:/Users/Quant/Documents/Programming/Projects/qompack-sp08`
(worktree, branch `fix/post-audit-code-round`, HEAD `6ca496f`). Line numbers are from that tree.

Counts: **7 MISMATCH · 0 MISSING · 55 OK**

Conventions: a plan field list written with braces (`Root{Hash, Chunks, …}`) is read as an
exhaustive claim about the type; a plan list of interface *methods it consumes* is read as a
subset claim and is verdicted OK, with the full real method set given in the Actual column.

---

## MISMATCH / MISSING rows

| # | Claim (short) | Verdict | Actual (file:line + exact real shape) |
|---|---|---|---|
| 1 | `func WriteAtomic(p string, b []byte) error` (Consumes, `internal/paths`) | **MISMATCH** | `internal/paths/atomic.go:105` — `func WriteAtomic(p string, b []byte, perm fs.FileMode) error`. Three parameters, not two; the third is a required `io/fs.FileMode`. Any SP-08 call written from the plan's signature will not compile. |
| 2 | Amendment (c), bullet 3: "add the matching `Mode` line and its two-direction note to `plans/00-ARCHITECTURE.md` §5.4" | **MISMATCH** | Both are **already in §5.4**. `plans/00-ARCHITECTURE.md:926` — `    Mode func() contract.Mode                                                          // SP-05 → SP-08`, inside the §5.4 `type Services struct`. The two-direction note is `:916-925`, and it already states the ordering the amendment proposes: *"New therefore constructs the monitor and assigns svc.Mode = monitor.Mode BEFORE it runs the bind loop, so a bind body may capture svc.Mode and call it later. Calling it DURING the bind body is still wrong: the monitor has not yet loaded state/contract.json, so it answers ModeFull for every project. Capture the func value; call it per event."* The doc is therefore not the party to amend — the **code** is the drifting party, and today it violates a normative §5.4 (see rows 3–4 of the OK table for the code evidence). The amendment's code edits stay valid; its doc edit is a no-op. |
| 3 | `ArgsDigest(raw json.RawMessage) (core.Hash, string)` listed inside the block of `internal/store` `Store`-interface methods (between `MarkSuperseded` and `AppendFileVersion`) | **MISMATCH** | `internal/store/tooluseindex.go:348` — `func ArgsDigest(raw json.RawMessage) (core.Hash, string)`. It is a **package-level function**, not a method on `Store`, and it is not on the interface (`internal/store/store.go:21-67`). `plans/00-ARCHITECTURE.md:1202` is explicit: *"ArgsDigest is a PACKAGE function, not a Store method — it is pure over its input and needs no store."* The signature and the quoted doc sentence are both exactly right; only the placement implies a method call `st.ArgsDigest(raw)` where the real call is `store.ArgsDigest(raw)`. |
| 4 | `PutResult{Root, Novel, Reused, Signature, NearDup}` | **MISMATCH** | `internal/store/types.go:47-66` — seven fields. The plan omits `Truncated bool` (`:62`, *"reports that the input exceeded MaxPutBytes and its tail was discarded. Put truncates rather than failing, because every producer of a Put is a hook and §2.3 permits a hook no exit code but 0"*) and `Redacted int` (`:65`, redact.Match spans replaced on the way in). Both are observations SP-08's hot path can surface and neither appears anywhere in the plan's Consumes block. `plans/00-ARCHITECTURE.md:1135-1143` lists all seven. |
| 5 | `NodeKind: KindToolUse KindToolResult KindAssistant KindUserPrompt KindFile KindSymbol KindDecision KindElimination KindSegment` | **MISMATCH** | `internal/dag/node.go:17-42` — the nine are correct and in that order (`KindToolUse NodeKind = iota` … `KindSegment` = 8), but the block ends with a **tenth** value, `KindInvalid` (`:41`), deliberately LAST. The consequence is load-bearing and the plan does not state it: *"NodeKind's zero value is KindToolUse, not 'unset'"* (`:38-39`), so `AddNode` validates kind against the NodeID prefix, and an `ObservedTool` chain built with a zero-value `Kind` looks like a genuine tool-use node. |
| 6 | `EdgeKind: EdgeSequence EdgeProduces EdgeConsumes EdgeSharedFile EdgeSharedSymbol EdgeSupersedes EdgeExplains EdgeControlOnly` | **MISMATCH** | `internal/dag/edge.go:12-36` — the eight are correct and in that order (`EdgeSequence EdgeKind = iota` = 0 … `EdgeControlOnly` = 7), but the block ends with a **ninth**, `EdgeInvalid` (`:35`), also last. `EdgeKind`'s zero value is therefore `EdgeSequence`, not "unset" (`:33-35`). |
| 7 | "builders.go … and `ObservedTool`'s own doc names 'observer (§5.7)' as its only production caller" | **MISMATCH** | The sentence is in the **file-level** comment, not `ObservedTool`'s doc: `internal/dag/builders.go:17-18` — *"observer (§5.7) is the only production caller and it holds a dag.Graph, not the concrete type"*. `ObservedTool`'s own doc (`:78-84`) says *"as the observer (§5.7) sees it in a PostToolUse payload plus whatever the analyzer already resolved for it"* — it names observer, but not as sole production caller. Separately, `§5.7` in that quoted code comment is itself wrong upstream: `plans/00-ARCHITECTURE.md:1022` is `### 5.7 internal/sketch`; observer is `### 5.21` (`:2124`). The plan quotes the code faithfully; it inherits the code's stale section reference. Cosmetic — no code changes. |

---

## OK rows

| # | Claim (short) | Verdict | Actual (file:line + exact real shape) |
|---|---|---|---|
| 8 | `hookio.Event` — 14 fields with the listed json tags | OK | `internal/hookio/event.go:18-37`. Field for field and tag for tag as the plan writes them, in that order. `Source` and `Trigger` carry doc comments (`:23`, `:25`) that the plan collapses into trailing `//` comments; same tags. |
| 9 | `Extra map[string]json.RawMessage \`json:"-"\`` | OK | `internal/hookio/event.go:36` — exact. Doc `:33-35`: *"holds every top-level key the struct tags above do not claim … nil when the payload contributed no unclaimed keys — not an empty, allocated map."* Also normative at `plans/00-ARCHITECTURE.md:803`. |
| 10 | `ReadEvent` fills `Extra` | OK | `internal/hookio/event.go:90` — `func ReadEvent(r io.Reader, limit int64) (Event, []byte, error)`; `:110-123` re-unmarshals `raw` into `map[string]json.RawMessage`, skips `claimedKeys` (built by reflection, `:39-42`), and assigns `ev.Extra = extra`. |
| 11 | `ipc.EncodeRequest` drops `Extra` (amendment (b) premise) | OK | `Event.Extra` is `json:"-"` (`internal/hookio/event.go:36`) and `ipc.Request.Event` is a plain `*hookio.Event` serialized by `encoding/json` (`internal/ipc/wire.go:78`, `internal/ipc/frame.go:49`). A daemon-side `e.Extra` is therefore always nil in production. Premise holds. |
| 12 | `hookio.Output{Continue, SuppressOutput, HookSpecificOutput, SystemMessage}` | OK | `internal/hookio/output.go:11-16` — exact, tags included. |
| 13 | `hookio.HSO{HookEventName, AdditionalContext, CustomInstructions}` | OK | `internal/hookio/output.go:20-26` — exact, tags included. |
| 14 | `func Empty() Output` | OK | `internal/hookio/output.go:46` — `func Empty() Output { return Output{} }`. |
| 15 | `Store` methods `Put`/`PutBytes`/`GetRoot`/`RecordToolUse`/`ToolUse`/`ToolUsesByPath`/`MarkSuperseded`/`AppendFileVersion`/`FileHistory`/`Segments`/`Stats`/`GC`/`Flush` | OK (subset) | `internal/store/store.go:21-67`. All thirteen match verbatim. The interface carries **eight more** the plan does not list: `GetChunk(ctx, h core.Hash) ([]byte, error)` (`:26`), `Open(ctx, root core.Hash) (io.ReadCloser, error)` (`:29`), `OpenSpan(ctx, root core.Hash, off, n int64) (io.ReadCloser, error)` (`:32`), `Has(h core.Hash) bool` (`:34`), `FileAt(ctx, path string, at time.Time) (FileVersion, error)` (`:48`), `ChangedSince(ctx, deps []core.Dep) ([]core.Dep, error)` (`:53`), `Search(ctx, q Query) ([]Hit, error)` (`:57`), `Close() error` (`:66`). |
| 16 | `PutOptions{Tool, Path, Canon, KeepRaw, Ephemeral}` | OK | `internal/store/types.go:32-44` — exactly five, in that order; `Canon canon.Options` (`:38`). |
| 17 | `NearDupInfo{PriorRoot, Jaccard, DeltaBytes}` | OK | `internal/store/types.go:70-77` — `PriorRoot core.Hash; Jaccard float64; DeltaBytes int64`. |
| 18 | `Root{Hash, Chunks, CanonBytes, RawBytes, Tokens}` | OK | `internal/store/types.go:19-29` — exactly five, in that order. |
| 19 | `Supersession{StatusOK, StatusSuperseded}` | OK | `internal/store/tooluse.go:17-24` — `type Supersession uint8`; `StatusOK Supersession = iota` (0), `StatusSuperseded` (1). The zero value is frozen by the golden fixture (`:12-17`). |
| 20 | `ToolUseRecord{ID, Session, Turn, TS, Tool, ArgsDigest, ArgsPreview, Root, Path, Bytes, Tokens, Signature, Status, SupersededBy, Ephemeral, Subagent}` | OK | `internal/store/tooluse.go:33-57` — all sixteen, in that exact order. `Subagent string` at `:56`, doc: *"\"\" for the main agent, or the subagent's name for a SubagentStop capture"*. The struct carries **no json tags**; the wire shape is supplied by `toolUseRecordWire` + custom `MarshalJSON`/`UnmarshalJSON` (`:68-122`). |
| 21 | `FileVersion{TS, Root, Turn, Bytes}` | OK | `internal/store/tooluse.go:126-131` — `TS core.UnixMilli \`json:"ts"\`; Root core.Hash \`json:"root"\`; Turn core.TurnIndex \`json:"turn"\`; Bytes int64 \`json:"bytes"\``. |
| 22 | `GCPolicy{RetainDays, RetainSessions, DryRun, Deadline}` | OK | `internal/store/gc.go:12-20` — `RetainDays, RetainSessions int; DryRun bool; Deadline time.Duration`. |
| 23 | `GCReport` fields (task item, not enumerated in the plan) | OK | `internal/store/gc.go:23-32` — `ScannedObjects, LiveObjects, DeletedObjects int; BytesFreed int64; Roots int; Duration time.Duration; Truncated bool`. |
| 24 | `Stats{Objects, Bytes, RawBytes, DedupRatio, ToolUses, Segments, Files, Sketches}` | OK | `internal/store/gc.go:36-46` (not `stats.go`) — `Objects int; Bytes int64; RawBytes int64; DedupRatio float64; ToolUses, Segments, Files int; Sketches map[string]int`. Exactly the eight named. |
| 25 | `SegmentLog`: `Open`/`Close`/`Current`/`Frontier` signatures | OK (subset) | `internal/store/segment.go:38-57` — all four verbatim: `Open(ctx, s Segment) (core.SegmentID, error)` (`:40`), `Close(ctx, id core.SegmentID, endTurn core.TurnIndex, feats map[string]float64) error` (`:42`), `Current(ctx, s core.SessionID) (Segment, error)` (`:48`), `Frontier(ctx, s core.SessionID) (core.TurnIndex, error)` (`:54`). Four more on the interface: `Get` (`:44`), `Range` (`:46`), `MarkEncoded` (`:52`), `Unencoded` (`:56`). |
| 26 | `Segment` fields (task item) | OK | `internal/store/segment.go:15-34` — `ID, Session, StartTurn, EndTurn, StartTS, EndTS, Features map[string]float64, Tokens, EncodedOnce, CheckpointSeq, Closed, BloomRef`, all json-tagged. |
| 27 | Amendment (a): `FSStore.canonOptions` honours the strip list only when non-empty | OK | `internal/store/put.go:249-251` — verbatim: `// A caller that supplied its own canon.Options wins: PutOptions.Canon is the per-call override` / `// the §5.8 shape provides for exactly this.` / `if len(o.Canon.Strip) > 0 {` / `opts.Strip = o.Canon.Strip` / `}`. Declared `func (s *FSStore) canonOptions(o PutOptions) canon.Options` at `:232`. |
| 28 | Amendment (a): `canonOptions` never reads `o.Canon.MinHash` | OK | `internal/store/put.go:236-240` — MinHash is built solely from `cc := s.cfg.Store.Canonicalize` (`:233`): `MinHash: sketch.MinHashOptions{Enabled: cc.MinHash.Enabled, Permutations: cc.MinHash.Permutations, NearDupThreshold: cc.MinHash.NearDupThreshold}`. `o.Canon.MinHash` appears nowhere in the function. |
| 29 | Amendment (a): `Canon.KeepDeltas` is derived from `PutOptions.KeepRaw`, not read from `Canon` | OK | `internal/store/put.go:235` — `KeepDeltas: o.KeepRaw,`. `o.Canon.KeepDeltas` is never read. |
| 30 | Amendment (a): `FSStore.nearDup` is the §8.1 item 3 detector and dies if MinHash is zeroed | OK | `internal/store/put.go:260-265` — `func (s *FSStore) nearDup(path string, root core.Hash, canonBytes int64, sig sketch.Signature) *NearDupInfo`, gated `if path == "" || !cc.MinHash.Enabled { return nil }`. Doc `:257-259`: *"This is §8.1's 'same test suite, one new failure' detector."* (The plan's "`supersede.go` depends on it" is a forward reference to an SP-08 file that does not exist yet — `find . -name 'supersede*.go'` returns nothing.) |
| 31 | Amendment (a): store's config has "the six classes" | OK | `internal/config/defaults.go:29` — `Strip: []string{"timestamps", "ansi", "pids", "addresses", "tmpPaths", "durations"}`. Six. (`internal/canon` knows eight; `crlf`/`paths` are always-on — row 39.) |
| 32 | `ToolUsesByPath` returns newest first | OK | `internal/store/tooluseindex.go:265-287` — `for i := len(ids) - 1; i >= 0 && len(out) < n; i--` (`:281`) walks the per-path id slice backwards, so results are most-recent-first; `limit > 0` truncates to the newest `limit` (`:277-279`). |
| 33 | `dag.Graph`: `AddNode(n Node) error`, `AddEdge(e Edge) error`, `Flush(ctx) error` | OK (subset) | `internal/dag/graph.go:35-59` — `AddNode(n Node) error` (`:36`), `AddEdge(e Edge) error` (`:37`), `Flush(ctx context.Context) error` (`:54`). Eight more on the interface: `Node(id NodeID) (Node, bool)` (`:39`), `Out(id NodeID) []Edge` (`:41`), `In(id NodeID) []Edge` (`:43`), `BackwardSlice(criteria []NodeID, o SliceOptions) (Slice, error)` (`:45`), `ForwardSlice` (`:47`), `CrossingEdges(pos int) int` (`:50`), `NodesAfter(pos int) []Node` (`:52`), `Compact(ctx) error` (`:56`), `Stats() GraphStats` (`:58`). `Tombstone`/`NeedsCompaction`/`Generation` live on the separate `Maintainer` (`:67-75`), reached by assertion. |
| 34 | `Node{ID, Kind, Turn, TS, Pos, Ref, Root, Tokens, Ephemeral}` | OK | `internal/dag/node.go:55-74` — exactly nine, in that order, all json-tagged. |
| 35 | `Edge{From, To, Kind, Weight, Turn}` | OK | `internal/dag/edge.go:44-52` — `From NodeID; To NodeID; Kind EdgeKind; Weight float32; Turn core.TurnIndex`. `Weight` is `float32`, not `float64`. |
| 36 | `func BuildToolUse(g Graph, o ObservedTool) error` / `BuildUserPrompt(g Graph, o ObservedPrompt) error` / `BuildSegment(g Graph, s SegmentSpec) error` | OK | `internal/dag/builders.go:146`, `:355`, `:518` — all three verbatim, free functions over `Graph`. |
| 37 | builders.go doc: "the one place in the repository that turns an observation of the transcript into nodes and edges" | OK | `internal/dag/builders.go:13-14` — verbatim: *"The §8.1 item 4 edge constructors: the one place in the repository that turns an observation of the transcript into nodes and edges."* |
| 38 | `ObservedTool` fields (15, incl. `Symbols []string` resolved by the caller, sorted+deduped by `BuildToolUse`) | OK | `internal/dag/builders.go:85-130` — same fifteen names as the plan, declared in a different order (`ToolUseID, PrevToolUseID, PrevTurn, Supersedes, Turn, TS, Pos, ResultPos, Tool, PathKey, Writes, Symbols, Root, Tokens, Ephemeral`). `:118-120`: *"Symbols are the symbol names this call touched, in any order: BuildToolUse sorts and deduplicates them."* `:81-84` confirms the caller resolves them because dag may not import `internal/symbols` (§3.2). |
| 39 | `ObservedPrompt{Turn, TS, Pos, Tokens, Ref}` | OK | `internal/dag/builders.go:334-347` — exactly those five, in that order. |
| 40 | `SegmentSpec{ID, PrevID, StartTurn, EndTurn, TS, StartPos, Tokens, Members}` | OK | `internal/dag/builders.go:492-510` — exact, in that order; `Members []NodeID` (`:509`). |
| 41 | nodeid.go doc: "Every consumer builds IDs through the constructors … silently produces two disconnected halves of the same graph" | OK | `internal/dag/nodeid.go:13-16` — verbatim. |
| 42 | NodeID constructors and their string formats | OK | All in `internal/dag/nodeid.go`, all routed through `newNodeID` (`:120-122`, `prefixOf(k) + ":" + sanitizeKey(key)`), prefixes at `:73-84`: `ToolUseNode(id core.ToolUseID) NodeID` → `"tooluse:<id>"` (`:127`); `ToolResultNode(id core.ToolUseID) NodeID` → `"toolresult:<id>"` (`:132`); `AssistantNode(t core.TurnIndex) NodeID` → `"assistant:"+strconv.Itoa(int(t))`, no session component (`:135-137`); `UserPromptNode(t core.TurnIndex) NodeID` → `"userprompt:<decimal turn>"`, no session component (`:142-144`); `FileNode(pathKey string) NodeID` → `"file:<pathKey>"`, and `:146-149` states it *"deliberately does NOT call paths.Key"*; `SymbolNode(pathKey, name string) NodeID` → `"symbol:<pathKey>#<name>"` (`:156-158`); `SegmentNode(id core.SegmentID) NodeID` → `"segment:<decimal id>"` (`:171-173`). Caveat worth knowing: `sanitizeKey` (`:212-235`) rewrites control/invalid-UTF-8 bytes and hash-truncates keys over 384 bytes, so a NodeID is not always a literal concatenation. |
| 43 | `dag.Open(dir, cfg, log)` | OK | `internal/dag/log.go:50` — `func Open(root string, cfg config.Config, log logging.Logger) (Graph, error)`. Three args in that order; the first is the PROJECT root. |
| 44 | `DefaultSliceOptions`, `BackwardSlice`, `SliceOptions.Thin` | OK | `internal/dag/slice.go:83-90` — `func DefaultSliceOptions(cfg config.Config) SliceOptions` returning `{Thin: cfg.Selection.Slicing == thinSlicingName, MaxNodes: DefaultMaxNodes, Decay: DefaultDecay, Deadline: DefaultDeadline}`. `internal/dag/traverse.go:260` — `func (g *graph) BackwardSlice(criteria []NodeID, o SliceOptions) (Slice, error)`. `Thin bool` at `slice.go:12`: *"drops EdgeControlOnly edges (§6.4 thin slicing; default true)"*; `withDefaults` deliberately does not default it (`slice.go:94-98`). |
| 45 | `sketch` — `(*CMS).Add`, `(*CMS).Estimate`, `(*HLL).Add`, `(*HLL).Cardinality`, `(*MisraGries).Add`, `(*MisraGries).Top`, `Signature.IsNearDup`, `Save` | OK | `internal/sketch/cms.go:129` `func (c *CMS) Add(key []byte, n uint32)`; `cms.go:151` `func (c *CMS) Estimate(key []byte) uint32`; `hll.go:102` `func (h *HLL) Add(key []byte)`; `hll.go:128` `func (h *HLL) Cardinality() uint64`; `misragries.go:196` `func (m *MisraGries) Add(key string, n int)`; `misragries.go:256` `func (m *MisraGries) Top(n int) []Counted`; `minhash.go:585` `func (s Signature) IsNearDup(o Signature, threshold float64) bool`; `io.go:69` `func Save(p string, s Sketch) error`. |
| 46 | `sketch.MinHashOptions{Enabled, Permutations, ShingleSize, NearDupThreshold}` | OK | `internal/sketch/minhash.go:121-141` — exactly those four, in that order; `Enabled bool`, `Permutations int`, `ShingleSize int`, `NearDupThreshold float64`. `:137-140` confirms the amendment's premise that `NearDupThreshold` *"is read by NO code in this package"*. |
| 47 | `sketch.Bloom` type name | OK | `internal/sketch/bloom.go:92` — `type Bloom struct`. |
| 48 | `canon.Class` is a string type; `"crlf"` is a valid `Class` | OK | `internal/canon/types.go:6` — `type Class string`. Eight constants at `:10-27`: `ClassTimestamps "timestamps"`, `ClassANSI "ansi"`, `ClassPIDs "pids"`, `ClassAddresses "addresses"`, `ClassTmpPaths "tmpPaths"` (camelCase), `ClassDurations "durations"`, `ClassCRLF "crlf"` (`:24`), `ClassPaths "paths"` (`:26`). So yes — `"crlf"` is a valid `Class`, and it is in `internal/config`'s enum too (`internal/config/config.go:60`). |
| 49 | `canon.Options{Strip []Class; KeepDeltas bool; MinHash MinHashOptions}` | OK | `internal/canon/types.go:59-66` — `Strip []Class; KeepDeltas bool; MinHash sketch.MinHashOptions`. The plan's unqualified `MinHashOptions` resolves: `internal/canon/classes.go:11` declares `type MinHashOptions = sketch.MinHashOptions` — an **alias, not a defined type** (`:8-10`: *"so callers configuring a canonicalization pass need not import sketch for the one field"*). Both spellings compile and are assignable. |
| 50 | `gateSet` nil-vs-empty `Strip` semantics; `alwaysOn` | OK | `internal/canon/classes.go:62-84`, doc verbatim at `:64-68`: *"A nil Strip means 'every class' — the documented meaning of the zero Options (see Options.Strip) and what a caller who has not thought about classes should get. A non-nil Strip, INCLUDING an empty one, means exactly those classes; that distinction is what lets a test ask for 'nothing optional' without also asking for 'nothing at all'. Either way the always-on structural classes are added."* Body: `if strip == nil { for _, c := range knownClasses { gate[c] = true }; return gate }` (`:71-76`), then `for _, c := range strip { gate[c] = true }` and `for c := range alwaysOn { gate[c] = true }` (`:77-82`). `internal/canon/classes.go:40` — `var alwaysOn = map[Class]bool{ClassCRLF: true, ClassPaths: true}`. The amendment's premise is exact. |
| 51 | `grammar.Sequitur`: `Append(s Symbol)`, `Rules() []Rule`, `Thrash(minUses int) []Rule` | OK (subset) | `internal/grammar/sequitur.go:8-23` — all three verbatim (`:10`, `:12`, `:15`). Four more on the interface: `Compressed() []Symbol` (`:18`), `Reset()` (`:20`), `MarshalBinary() ([]byte, error)` (`:21`), `UnmarshalBinary([]byte) error` (`:22`). Plan's "stub in wave 2; must be tolerated" is right — `New()` returns `stubSequitur{}` (`:32-33`). |
| 52 | `grammar.Symbol`, `RuleID`, `Rule` (ID, Uses, …), `Warning`, `FormatWarning` | OK | `internal/grammar/types.go:8` `type Symbol string`; `:11` `type RuleID int`; `:15-27` `Rule{ID RuleID; Body []Symbol; Uses int; Expansion []Symbol; Span int}`; `:32-41` `Warning{Rule Rule; Repeats int; Message string; Turns []core.TurnIndex}`; `internal/grammar/formatwarning.go:21` `func FormatWarning(w Warning) string`. |
| 53 | `tokens.Estimator`: `EstimateRoot(ctx, chunks []core.ChunkRef, c Class) core.Tokens`, `EstimateString(s string, c Class) core.Tokens` | OK (subset) | `internal/tokens/estimate.go:13-24` — both verbatim (`:19`, `:15`). Three more on the interface: `Estimate(b []byte, c Class) core.Tokens` (`:14`), `Calibrate(observed, estimated core.Tokens)` (`:22`), `Factor() float64` (`:23`). |
| 54 | `tokens.Classify(tool, path string, b []byte) Class`; `Class` constants | OK | `internal/tokens/classify.go:46` — `func Classify(tool, path string, b []byte) Class`. `internal/tokens/class.go:6` `type Class uint8`; `:9-17` the seven: `ClassProse Class = iota`, `ClassCode`, `ClassJSON`, `ClassDiff`, `ClassImage`, `ClassPDF`, `ClassBinary`. |
| 55 | `core.HashBytes(domain string, b []byte) Hash`; `Hash.String()`/`Short()`; `ParseHash` | OK | `internal/core/hash.go:64`, `:84` (`return hashTextPrefix + hex.EncodeToString(h[:])`), `:88` (`return hex.EncodeToString(h[:])[:12]` — 12 hex chars, no prefix), `:97` `func ParseHash(s string) (Hash, error)`. |
| 56 | `core.DomainArgs` value | OK | `internal/core/hash.go:47-48` — `// DomainArgs is the domain for tool-argument digests.` / `DomainArgs = "qompack.args.v1"`. |
| 57 | `core.Clock` interface | OK | `internal/core/clock.go:8-11` — `Now() time.Time; Since(time.Time) time.Duration`. Helpers: `SystemClock() Clock` (`:19`), `NowMilli(c Clock) UnixMilli` (`:22`). |
| 58 | `core` scalar types: `UnixMilli`, `TurnIndex`, `SegmentID`, `ToolUseID`, `SessionID`, `Tokens` | OK | `internal/core/ids.go:6-23` — `SessionID string`, `ToolUseID string`, `TurnIndex int`, `SegmentID int` (1-based), `CheckpointSeq int`, `DecisionID string`, `Tokens int`, `UnixMilli int64`. |
| 59 | `core.ChunkRef` | OK | `internal/core/ids.go:33-36` — `Hash Hash \`json:"hash"\`; Len int \`json:"len"\``. `store.ChunkRef` is an alias of it (`internal/store/types.go:11`). |
| 60 | `core.ErrNotImplemented` | OK | `internal/core/errors.go:10` — `ErrNotImplemented = errors.New("qompack: not implemented")`; helper `IsNotImplemented(err error) bool` at `:30`. |
| 61 | `paths.Norm(projectRoot, p string) (string, error)`; `paths.Key(p string) string` | OK | `internal/paths/norm.go:19` and `:93` (`func Key(p string) string { return KeyFold(p, DefaultFold()) }`). Both exact. |
| 62 | `config.Config.Get(dotted string) (any, bool)`; `Defaults() Config` | OK | `internal/config/config.go:280` — `func (c Config) Get(dotted string) (any, bool)` (value receiver). `internal/config/defaults.go:14` — `func Defaults() Config`. |
| 63 | `Config.Store.Canonicalize{Enabled, Strip, MinHash{Enabled, Permutations, NearDupThreshold}}` | OK | `internal/config/config.go:57-69` — `CanonicalizeCfg{Enabled bool; Strip []string; MinHash MinHashCfg}`; `MinHashCfg{Enabled bool; Permutations int; NearDupThreshold float64}`. Note `Strip` is `[]string`, not `[]canon.Class` — `canon.OptionsFrom` converts (`internal/canon/classes.go:93-99`), as does `FSStore.canonOptions` (`internal/store/put.go:242-247`). |
| 64 | `Config.Store.Retention{Days, Sessions}` | OK | `internal/config/config.go:52-55` — `RetentionCfg{Days int; Sessions int}`. |
| 65 | `Config.Runtime.HotPath.MaxPayloadBytes` | OK | `internal/config/runtime.go:35-39` — `HotPathCfg.MaxPayloadBytes int` (default `1048576`, `internal/config/defaults.go:125`; validated `>= 4096`). |
| 66 | `logging.Logger.Debug/Info/Warn/Error/Loud(msg string, kv ...any)`; `Nop()` | OK (subset) | `internal/logging/logger.go:20-32` — all five exact (`:24-31`), plus `With(kv ...any) Logger` (`:23`) the plan does not list. `Loud`'s doc (`:28-31`): *"writes to the day log AND to <dir>/LOUD.log … Never silent (§12)."* `internal/logging/logger.go:83` — `func Nop() Logger { return logger{s: nil, minLevel: Loud} }`. |
| 67 | `obs.Registry.Hist(name string) Histogram`, `Counter(name string) Counter`; `Counter` increment name; `Timed` | OK (subset) | `internal/obs/registry.go:39-51` — `Hist(name string) Histogram` (`:40`), `Counter(name string) Counter` (`:41`), plus `Gauge` (`:42`), `Snapshot() Snapshot` (`:44`), `CheckBudgets(cfg config.Config) []BudgetBreach` (`:48`), `Persist(l paths.Layout) error` (`:50`). Counter's increment is **`Add(n int64)`**, not `Inc()` — `internal/obs/counter.go:7-10` (`Add(n int64); Value() int64`). `internal/obs/registry.go:178` — `func Timed(h Histogram, f func() error) error`. `Histogram` is `Observe(d time.Duration); Snapshot() HistSnapshot; Reset()` (`internal/obs/hist.go:13-17`). |
| 68 | `daemon.Options` fields (ProjectRoot, Log, Metrics, Clock, Sketches, Sched, …) | OK | `internal/daemon/options.go:28-52` — `ProjectRoot string; Cfg config.Config; Log logging.Logger; Metrics obs.Registry; Clock core.Clock; Store store.Store; Ledger negknow.Ledger; Sketches *SketchSet; Graph dag.Graph; Grammar grammar.Sequitur; Sched scheduler.Runtime; Checkpoints checkpoint.Writer`, plus unexported `handlers map[ipc.Op]ipc.Handler` (`:46`) and `binds []func(*Services)` (`:51`). No `Mode` — matching the plan. |
| 69 | `Options.Bind` receiver kind and signature | OK | `internal/daemon/options.go:101` — `func (o *Options) Bind(fn func(*Services))`. **Pointer** receiver, as §5.4 requires (`plans/00-ARCHITECTURE.md:894-897`: *"Both Handle and Bind take a POINTER receiver … A value receiver would mutate a copy and register nothing"*). |
| 70 | `Options.Handle` | OK | `internal/daemon/options.go:75` — `func (o *Options) Handle(op ipc.Op, h ipc.Handler)`. Siblings `Handler(op) (ipc.Handler, bool)` (`:83`) and `Ops() []ipc.Op` (`:89`). |
| 71 | `Services` has **no** `Mode` field (amendment (c) premise) | OK | `internal/daemon/options.go:110-131` — `Store, Ledger, Sketches, Graph, Grammar, Sched, Checkpoints`, then the nine function seams `ObserveTool, ObservePrompt, ObserveStop, SessionStart, SessionEnd, PreCompact, Rehydrate, MCPInitialized, StatusExtra`. No `Mode`. Confirmed absent — and note that this is a **code-vs-doc drift**, since `plans/00-ARCHITECTURE.md:926` already declares it (row 2). |
| 72 | `daemon.New` signature | OK | `internal/daemon/daemon.go:209` — `func New(o Options) (Daemon, error)`. |
| 73 | Inside `New`: the bind loop runs **before** `contract.NewMonitor` (amendment (c) premise) | OK | `internal/daemon/daemon.go:232-241`, verbatim: `for _, bind := range o.binds {` / `bind(svc)` / `}` / `DeclareProducers(svc)` / (blank) / `statePath := filepath.Join(paths.Of(o.ProjectRoot).State, "contract.json")` / `monitor := contract.NewMonitor(o.Log, o.Metrics, statePath)` / `for _, a := range contract.StandardAssertions() {` / `_ = monitor.Register(a)` / `}`. The monitor is assigned into the unexported `d.monitor` at `:251` and passed to `newIdleController` at `:260`; it is on neither `Options` nor `Services` nor the `Daemon` interface, exactly as the plan states. |
| 74 | `Daemon` interface has `Idle()` and `Registry()` | OK | `internal/daemon/daemon.go:63-75` — `Run(ctx) error`; `Registry() *SessionRegistry` (`:67`); `Drain(ctx) (int, error)` (`:70`); `Idle() IdleController` (`:72`); `Stop(ctx) error` (`:74`). |
| 75 | `IdleController.Register` signature | OK | `internal/daemon/idle.go:33` (interface) and `:93` (impl) — `Register(name string, prio int, fn func(ctx context.Context) error)`. No error return; re-registering a name replaces in place. |
| 76 | `SketchSet` exported members | OK | `internal/daemon/sketchset.go:40-49` — `Tried *sketch.Bloom; Touch *sketch.CMS; Explore *sketch.HLL; Top *sketch.MisraGries` (plus unexported `mu`, `dirty`). Note the Misra-Gries field is **`Top`**, which the plan's `observer.Options` receives as `Hot *sketch.MisraGries` — a rename at the wiring boundary, not a type mismatch. Accessors `Read(fn)` (`:68`) and `Write(fn)` (`:78`, sets `dirty`). |
| 77 | `SketchSet.Save` dirty gating | OK | `internal/daemon/sketchset.go:135-159` — `func (s *SketchSet) Save(root string, log logging.Logger)`; `if !s.dirty { return }` after taking the write lock (`:144-146`); saves `touch.cms` and `explore.hll` only, clears `dirty`. Doc `:131-134`: *"sketches/tried.bloom is never written here (§3.3: it may be replaced only by negknow.RebuildBloom) — Save skips it unconditionally, on every call, dirty or not."* |
| 78 | `handleFlush` sequence | OK | `internal/daemon/handlers.go:702-704` delegates to `flushRoute` (`:713`). Order at `:714-747`: `resolveEvent(req)` → `core.NowMilli(d.clk)` → `d.registry.End(ev.SessionID, now)` → `d.ing.CloseSession(ev.SessionID)` → `if d.svc.SessionEnd != nil && d.monitor.Mode().MayRecord() { d.svc.SessionEnd(ctx, *ev) }` (`:723-727`) → `contract.WriteMarker(d.root, ev.SessionID, now)` (`:729`) → `if d.svc.Sketches != nil { d.svc.Sketches.Save(d.root, d.log) }` (`:733-735`) → `d.Drain(ctx)` (`:741`, skipped when replayed by Drain itself). Matches the doc comment at `:700-701`. Note `SessionEnd` is gated on **`MayRecord()`**, not `MayAct()` (`:717-722` explains why). |
| 79 | `resolveEvent` does **not** populate `Event.Extra` from `req.Raw` (amendment (b) premise) | OK | `internal/daemon/handlers.go:96-105`, whole body: `func resolveEvent(req ipc.Request) *hookio.Event { if req.Event != nil { ev := *req.Event; if ev.SessionID == "" { ev.SessionID = req.Session }; return &ev }; return &hookio.Event{SessionID: req.Session} }`. It copies the event and defaults `SessionID` only. `req.Raw` is never read here. Premise exact. |
| 80 | `handleObserveTool` / `handleObserveStop` / `handleObservePrompt` and the seam call sites | OK | `internal/daemon/handlers.go:330-332` and `:336-338` both delegate to `acceptHotPathEvent` (`:344-365`): `registry.Touch` unconditional (`:353`), then `if !d.monitor.Mode().MayRecord() { return ipc.Response{OK: true} }` (`:355-357`), then `ipc.EncodeRequest` + `d.ing.Accept`. **Neither calls `svc.ObserveTool`/`svc.ObserveStop` directly** — the worker pool (`runIngested`) does, asynchronously, and it is where `decodeSubagent` is applied (`:334-335`). `handleObservePrompt` (`:372-405`) is the synchronous one: `svc.ObservePrompt` at `:402-404`, gated `!= nil && mode.MayAct()`, via `callObservePromptWithDeadline` (`:413`). The other three of the five: `svc.SessionStart` at `:509-514` (gated `mode.MayAct()`), `svc.SessionEnd` at `:723-727` (gated `MayRecord()`), `svc.PreCompact` at `:648-665` (gated `MayAct()`, SP-10's). |
| 81 | `internal/cli/hookclient.go`'s `rawExtras` builds `{"subagent":true}` for `observe stop --subagent` | OK | `internal/cli/hookclient.go:362-375` — `func rawExtras(op ipc.Op, args []string, ev hookio.Event) json.RawMessage`; `case ipc.OpObserveStop: if hasFlag(args, "--subagent") { b, _ := json.Marshal(map[string]bool{"subagent": true}); return b }` (`:364-368`); `case ipc.OpCheckpoint: if ev.Trigger != "" { b, _ := json.Marshal(map[string]string{"trigger": ev.Trigger}); return b }` (`:369-373`). Called once, at `:243` — `Reply: spec.reply, Event: &ev, Raw: rawExtras(spec.op, args, ev)`. |
| 82 | `decodeSubagent` | OK | Lives in **`internal/daemon/handlers.go:109-118`**, not `internal/cli`: `func decodeSubagent(raw json.RawMessage) bool` — unmarshals `struct{ Subagent bool \`json:"subagent"\` }` from `req.Raw`, ignoring the error, so a missing/unparseable Raw reports false. The amendment's "`decodeSubagent` is untouched" holds: adding an `"agent"` key to the same object does not change what this reads. |
| 83 | `runDaemon` — how `opts` is declared and Log/Metrics/Clock assignment order | OK | `internal/cli/daemon.go:81-90`, verbatim: `opts := daemon.NewOptions(root, cfg)` / `opts.Log = log` / `opts.Metrics = reg` / `opts.Clock = core.SystemClock() // §6.1's own "connection deadlines are always real wall-clock time" rule applies to the daemon's own lifecycle clock too.` / (blank) / `d, err := daemon.New(opts)` / `if err != nil {` / `log.Loud("daemon: could not construct", "err", err.Error())` / `return nil` / `}`. `opts` is a **value**, not a pointer — SP-08's `Bind` call must therefore be `opts.Bind(…)` (auto-addressed) or pass `&opts`. |
| 84 | `git grep -n 'daemon\.New(' -- internal cmd test tools ':!*_test.go'` | OK | Exactly **one** hit: `internal/cli/daemon.go:86:	d, err := daemon.New(opts)`. Unfiltered (`git grep -n 'daemon\.New('` across the repo) adds only plan prose (`plans/V3-SP-08-observer-l0.md:2480,2483,2495`, `plans/V4-SP-12…`, `plans/V4-SP-13…`, `plans/WAVE2-PLUS-SCOPE-AUDIT.md:399`) and one line inside an archived review diff (`plans/sdd/V2-SP-05-daemon-ipc-and-hot-path/review-task5.diff:965`). No test file constructs a daemon through `daemon.New(` today. |
| 85 | `ipc.EncodeRequest` / `ipc.DecodeRequest` | OK | `internal/ipc/frame.go:49` — `func EncodeRequest(req Request) ([]byte, error)`; `:60` — `func DecodeRequest(line []byte) (Request, error)`. |
| 86 | `ipc.Request.Raw` field | OK | `internal/ipc/wire.go:69-80` — `Request{Op Op \`json:"op"\`; Session core.SessionID \`json:"s"\`; TS core.UnixMilli \`json:"t"\`; Reply bool \`json:"r,omitempty"\`; Event *hookio.Event \`json:"e,omitempty"\`; Raw json.RawMessage \`json:"x,omitempty"\`}`. `Raw` is wire key **`"x"`**. |
| 87 | `TestDecodeRequestRoundTrip` exists (amendment (b) asks to extend it) | OK | `internal/ipc/frame_test.go:123` — `func TestDecodeRequestRoundTrip(t *testing.T)`; doc `:121-122`: *"asserts DecodeRequest(EncodeRequest(r)) reproduces r exactly."* Referenced as the shape source by `internal/ipc/client_test.go:554`. |
| 88 | `contract.Mode` and its constants | OK | `internal/contract/mode.go:4` — `type Mode uint8`; `:14-16` — `ModeFull Mode = iota; ModeDegradedPassive; ModeOff`. All three exist with the plan's spellings. Predicates: `MayAct()` = `m != ModeDegradedPassive && m != ModeOff` (`:54`), `MayRecord()` = `m != ModeOff` (`:61`). |
| 89 | `contract.NewMonitor` signature and `Monitor.Mode` | OK | `internal/contract/monitor.go:108` — `func NewMonitor(log logging.Logger, m obs.Registry, statePath string) Monitor` (returns the **interface**). `:112` seeds `mode: ModeFull`. `Monitor.Mode() Mode` at `internal/contract/monitor.go:34`. The amendment's claim that `NewMonitor` reads only `o.ProjectRoot`, `o.Log` and `o.Metrics` holds at the `daemon.New` call site: `statePath` is derived purely from `paths.Of(o.ProjectRoot).State` (`internal/daemon/daemon.go:237`) and no bind produces any of the three. |
| 90 | `negknow.Ledger.RefreshStaleness(ctx, store)` exists | OK | `internal/negknow/ledger.go:55` — `RefreshStaleness(ctx context.Context, s store.Store) ([]string, error)`. Doc `:53-54`: *"compares every active record's depends_on hashes against s's current file versions and flips changed ones to stale. It returns the flipped ids."* Stub impl at `:109` returns `core.ErrNotImplemented` (SP-09 owns the real one). |
| 91 | `symbols.Extractor` and what `Extract` returns | OK | `internal/symbols/extractor.go:4-13` — `Extract(path string, b []byte) []Symbol` (`:7`), `Enclosing(path string, b []byte, off int) (Symbol, bool)` (`:10`), `References(b []byte, names []string) map[string]int` (`:12`). Element type `symbols.Symbol` at `internal/symbols/types.go:5-17` — `Name string; Kind string; Line int; Offset int; Len int`. `Name` exists and is the plan's `SymbolLister.Names` source. |
| 92 | `scheduler.Runtime.Observe` and `Features` fields | OK | `internal/scheduler/runtime.go:23` — `Observe(ctx context.Context, f Features, at core.TurnIndex) ChangepointState`. `internal/scheduler/types.go:95-106` — `Features{PathJaccard float64; ToolShift float64; LexicalCohesion float64; GapSeconds float64; TodoTransition float64}`. Five fields, no `Turn`/`TS` — consistent with the plan's `FeatureSample` carrying `Turn`/`TS` for the daemon to pass as `Observe`'s `at` argument. |
| 93 | `checkpoint.Writer` | OK | `internal/checkpoint/checkpoint.go:17-33` — `Begin(ctx, s core.SessionID, parent core.CheckpointSeq, src SourceSet) (*Draft, error)`; `Advance(ctx, d *Draft, segs []core.SegmentID) (core.TurnIndex, error)`; `Finalize(ctx, d *Draft, budget core.Tokens) (Ref, error)`; `Abort(d *Draft) error`. |
| 94 | `observer.Observer` interface, five methods — "§5.21, normative and unchanged" | OK | `internal/observer/observer.go:58-74`. Spelled with the package's own aliases (`e Event`, `(Output, error)`), but `internal/observer/observer.go:48-53` declares `type ( Event = hookio.Event; Output = hookio.Output )` — **aliases, not defined types**, so the plan's `hookio.Event`/`hookio.Output` spelling is type-identical. Matches `plans/00-ARCHITECTURE.md:2127-2133` verbatim. |
| 95 | `func Tombstone(rec store.ToolUseRecord) string` | OK | `internal/observer/tombstone.go:34-37` — real, not a stub: `fmt.Sprintf("[cleared: sha256:%s · %s · %s %s · re-expandable]", rec.Root.Short(), humanBytes(rec.Bytes), rec.Tool, rec.Path)`. |
| 96 | `Signals{TodoCompleted, TestPassed, GitCommit bool; Paths []string}`; `ExtractSignals` | OK | `internal/observer/signals.go:7-16` — exactly those four. `:28` — `func ExtractSignals(e Event) Signals { return Signals{} }` (SP-01 stub returning the zero value; `:22-27` explains why a plausible-looking value would violate rule 2). |
| 97 | The `observer.Options` SP-08 widens already exists with the plan's names | OK | `internal/observer/observer.go:83-102` — `ProjectRoot string; Cfg config.Config; Store store.Store; Graph dag.Graph; Grammar grammar.Sequitur; Tokens tokens.Estimator; Log logging.Logger; Metrics obs.Registry; Clock core.Clock`. Every name the plan reuses matches; the plan's additions (`Touch`, `Explore`, `Hot`, `Symbols`, `Rehydrate`, `Mode`, `OnSignals`, `OnFeatures`) are pure widening under Rule W-3, as `:81-82` and `plans/00-ARCHITECTURE.md:2135-2137` both anticipate. `func New(o Options) (Observer, error)` at `:107`. |
| 98 | `observertest.RunObserverSuite` signature | OK | `internal/observer/observertest/suite.go:40` — `func RunObserverSuite(t *testing.T, name string, factory func(t *testing.T) observer.Observer)`. |
| 99 | `skipIfStub` mechanism | OK | `internal/observer/observertest/suite.go:100-115` — `isStub` probes `factory(t).OnToolUse(ctx, toolUseEvent())` and reports `core.IsNotImplemented(err)`; `skipIfStub` calls `t.Skip(ruleW1SkipMsg)`. `ruleW1SkipMsg` is the exact literal `"behaviour: implementation is a stub (Rule W-1)"` (`:35`), grepped for by `devtool lint`'s stubskips sub-check — so a real `OnToolUse` that no longer returns `ErrNotImplemented` is what flips the behaviour block on. |
| 100 | The behaviour case names | OK | `internal/observer/observertest/suite.go:71-80` — eight: `post_tool_use_never_blocks_the_tool_call`, `user_prompt_capture_never_blocks_the_prompt`, `session_start_branches_on_source`, `stop_and_subagent_stop_are_both_accepted`, `session_end_flushes_without_reporting_an_error`, `every_entry_point_tolerates_a_malformed_event`, `replaying_one_event_twice_is_not_an_error`, `extract_signals_detects_todo_test_and_git`. The `/shape` block (`:43-65`) runs unconditionally and accepts nil or any of four sentinels (`requireKnownError`, `:86-96`). |
| 101 | `suite_test.go` factories | OK | `internal/observer/observertest/suite_test.go:49-71` — `TestObserverSuite_ShapePassesAgainstStub` runs two factories, each inside its own `t.Run` wrapper (`:45-48` explains why: Rule W-1's skip would otherwise abort the siblings): `"fake-stub"` returning `fakeStubObserver{}` (`:19-39`) and `"observer.New-stub"` calling `observer.New(observer.Options{ProjectRoot: t.TempDir(), Cfg: config.Defaults(), Log: logging.Nop(), Metrics: obs.New(core.SystemClock()), Clock: core.SystemClock()})`. |
| 102 | §3.2's allow-table lists `observer`'s permitted imports as "hookio store chunk canon sketch dag grammar negknow tokens" plus foundation | OK | `plans/00-ARCHITECTURE.md:409`, row verbatim: `\| `observer` \| `hookio` `store` `chunk` `canon` `sketch` `dag` `grammar` `negknow` `tokens` \|`. The table header establishes foundation (`core paths config logging obs`) as implicit for every row. Enforced in code identically: `tools/devtool/importrules.go:90` — `"observer": {"hookio", "store", "chunk", "canon", "sketch", "dag", "grammar", "negknow", "tokens"},`. `:438` adds the negative: *"`observer` must not import `scheduler` or `checkpoint`."* The plan's realized-set claim (`core paths config logging obs hookio store canon sketch dag grammar tokens`) is a legal subset. |
| 103 | §5.4 documents `Services` | OK | `plans/00-ARCHITECTURE.md:926-943` — the full `type Services struct` block with per-field owner annotations, preceded by the two-direction note at `:916-925`. (See row 2: it already carries `Mode`.) |
| 104 | §5.8 does **not** document `PutOptions.Canon` semantics | OK | `plans/00-ARCHITECTURE.md:1128-1134` — `PutOptions` is listed with `Canon canon.Options` and no accompanying prose; grepping §5.8 (`:1116-1300`) for `canon`/`strip`/`minhash` returns only the field itself (`:1131`), `Root.CanonBytes` (`:1123`), the `ArgsDigest` canonicalization sentence (`:1204`) and `Deps.Canon` (`:1269`). Nothing states nil-vs-empty `Strip` or the MinHash gate. The amendment's bullet asking to add that note is correct and still needed. |
| 105 | The three tests the amendment adds do not exist yet | OK | `git grep -n 'TestCanonOptions_EmptyStrip\|TestResolveEvent_RestoresRawExtras\|TestServicesModeIsAssignedBeforeBinds' -- internal` returns nothing. All three are genuinely new. |
| 106 | "nothing outside `internal/store` can reach `FSStore.canonOptions`" (amendment rationale) | OK | `canonOptions` is unexported on the unexported-return path (`internal/store/put.go:232`); the only caller is `Put`'s canonicalization step, `internal/store/put.go:119` — `cr, err := s.deps.Canon.Run(o.Tool, o.Path, red, s.canonOptions(o))`. No exported seam accepts a `canon.Options` that bypasses it. |

---

### Notes for the implementer (not verdicts)

- `plans/00-ARCHITECTURE.md:1202` already says `ArgsDigest` is a package function; the plan's own
  §5.8 quote (*"SP-08 calls this; nothing else may re-derive it"*) is verbatim from
  `internal/store/tooluseindex.go:340-341`. Only the plan's *placement* of the line inside the
  interface block is misleading.
- `supersede.go`, `prompt.go`, `tooluse.go` and `internal/daemon/observer_ops.go` are files SP-08
  creates; none exists on this tree. Their mention in the amendment section is forward reference,
  not a claim about shipped code, and is not counted as a mismatch.
- `internal/canon/classes.go:38` says *"Neither is one of Appendix C's six store.canonicalize.strip
  values"* about `crlf`/`paths`, while `internal/config/config.go:60`'s enum admits all eight
  (`timestamps|ansi|pids|addresses|tmpPaths|durations|crlf|paths`). That is a pre-existing
  internal inconsistency in the shipped code, not a plan claim — but it means a project config
  *can* list `crlf` in `strip`, which `gateSet` would then treat as redundant with `alwaysOn`.
