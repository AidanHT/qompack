# Task 4 — the contract monitor: producers, history, marker, sentinel, real G9.3 assertions (the plan's "Commit 5")

You are extending the existing `internal/contract` package in `github.com/qompack/qompack` (worktree: `C:/Users/Quant/Documents/Programming/Projects/qompack-sp05`, branch `feat/sp05-daemon-ipc-and-hot-path`). Tasks 1–3 delivered internal/ipc (transport, state record; also exported `contract.ParseMode` and added `Mode.MayAct`/`MayRecord`) and the daemon building blocks. This task delivers the full contract-monitor observation layer. The daemon wiring that CALLS it (WriteMarker from routes, RunAll at session start) is the NEXT task — this task is pure `internal/contract` + its tests.

Read IN ORDER:
1. `.superpowers/sdd/V2-SP-05-daemon-ipc-and-hot-path/sp01-inventory.md` — SP-01 inventory; shipped spellings win.
2. `.superpowers/sdd/V2-SP-05-daemon-ipc-and-hot-path/plan-context.md` — mission, out-of-scope, global rules.
3. `.superpowers/sdd/V2-SP-05-daemon-ipc-and-hot-path/task-4-spec.md` — verbatim requirements: the contract implementation spec (producers.go/history.go/marker.go/sentinel.go/monitor.go/assertions.go with the nine-assertion table and the `gated` wrapper), the contract test table, and the commit checklist (labelled "Commit 5" — that is THIS task's commit).
4. ALL existing `internal/contract/` source including tests, `internal/contract/contracttest/` (conformance suite — find what currently skips), `test/guards/contract_test.go` (pins you must not break), and `internal/testutil` (frozen-fixture mechanism — check which fixtures are frozen before touching any).

## Binding rulings (controller decisions amending the plan text)

- **`History` stays the shipped INTERFACE** (`Saw`/`LastSeen`/`Record`/`Sessions` — pinned by contracttest.RunHistorySuite). You build its first real implementation: `type SessionHistory struct` — an exported concrete struct carrying the plan's History fields with JSON tags as the plan spells them (`version`, `sessions`, `last_session_id`, `last_marker_ts`, `starts_without_marker`, `last_precompact_ts`, `last_precompact_session`, `precompact_wall_ms` (64 newest), `precompact_timeout_ms`, `precompact_instr` (≤256 chars), `awaiting_compact_start`, `sentinel`, `mcp_initialized`, `clean_runs`, `mode`, `degraded_reason`, `degraded_since`, `last` (≤9 results)) — with the Go field for the sessions count spelled `SessionCount int` `json:"sessions"` to avoid colliding with the interface's `Sessions()` method — PLUS a `Seen map[ID]core.UnixMilli` `json:"seen,omitempty"` field backing the interface methods (`Saw`/`LastSeen` read it, `Record` writes it). `*SessionHistory` implements `History`; wire contracttest's RunHistorySuite factory to it so its Rule W-1 skips flip off.
- **Persistence path**: `LoadHistory(path string) *SessionHistory` / `SaveHistory(path string, h *SessionHistory) error` operate on `<root>/.qompack/state/history.json` — NOT state/contract.json (that file remains the monitor's own persisted state; two schemas on one path would corrupt each other). The daemon passes the path; contract also exports `HistoryPath(projectRoot string) string`. LoadHistory returns a zero SessionHistory with Version 1 on ANY error (missing file is the first-run case). SaveHistory via `paths.WriteAtomic(p, b, 0o600)` creating the parent dir if needed (mirror monitor.persist's approach).
- **Assertions read the concrete record**: `Env.History` is the interface; each check that needs the rich fields type-asserts `h, ok := e.History.(*SessionHistory)`; `!ok` or nil ⇒ "no observation yet" ⇒ OK result (never a failure, never a panic). Checks MUTATE the SessionHistory via the pointer exactly where the plan's table says (StartsWithoutMarker++, clearing AwaitingCompactStart, Sentinel.Chances tracking is done by the daemon scan — read the table carefully); the daemon persists after RunAll (next task's job).
- **Monitor mechanics are SP-01's — extend, don't rewrite**: keep Register/Degrade/Restore/Report and the persisted `state` shape at state/contract.json untouched. Extend `RunAll` with exactly two capabilities: (1) runtime-mode handling — `e.Cfg.Runtime.Mode`: "off" → return `(nil, ModeOff)` immediately, zero assertions executed; "passive" → run assertions for reporting then force ModeDegradedPassive (no Degrade side effects beyond what a critical run would do — check the plan text: it forces the MODE; use a persisted forced state or return override — keep it simple: run assertions normally, then return ModeDegradedPassive as the mode and make Mode() reflect it while forced; document your choice); "full" → run for reporting, force ModeFull; "auto"/"" → current behavior. (2) per-assertion panic recovery: a panicking Check yields `Result{ID, OK:false, Severity:SevWarn, Observed:"assertion panicked"}` and never degrades the session. KEEP SP-01's deliberate every-critical-run re-loud Degrade behavior (do NOT make it reason-change-only; the plan's TestDegradeIsIdempotent row is adapted: assert two degrades produce Loud lines both times — same or different reason — and the persisted state stays consistent).
- **StandardAssertions keeps its shipped declared severities** (CPreCompactTiming declared SevWarn, CMCPRegistered declared SevInfo, others as shipped). Real checks return OBSERVED severities per the plan's table — notably precompact.has_time_to_write returns a SevCritical Result when p99 ≥ 100% of timeout (RunAll reads Result.Severity, so this degrades even though the declared severity is SevWarn; that asymmetry is the package's documented design). Keep the `notYetImplementedObserved` literal ("not-yet-implemented") — it is pinned by test/guards and goldens.
- **`gated` wrapper** exactly per the spec: producer absent → `Result{OK:true, Severity:SevInfo, Expected:desc, Observed:"not-yet-implemented"}` short-circuiting BEFORE the check body runs. All nine constructed through it. producers.go: package-level mutex-guarded map, `DeclareProducer(id)`, `HasProducer(id) bool`, `ResetProducers()` (tests only — used with t.Cleanup in every test that declares).
- **Do not break pinned tests**: run the full existing contract test files + golden_test.go + contracttest + `go test ./test/guards/...` before AND after. If a shipped contract test's expectation genuinely conflicts with a required new behavior, adapt the test ONLY if it is not a frozen fixture / guard, and record it in your report. Frozen fixtures (see testutil's frozen list) must not change — if you believe one must, STOP and report BLOCKED.
- Marker: `MarkerPath(root)` = `<root>/.qompack/run/marker.json`; `WriteMarker(root, sess, now)` writes `{"session":"<id>","ts":<ms>}` via WriteAtomic. Never called by anything in THIS task except tests (the daemon routes call it next task).
- Sentinel: exactly per spec — `MintSentinel` token `"qompack-contract-" + core.HashBytes("qompack.sentinel.v1", []byte(string(sess)+strconv.FormatInt(int64(now),10))).Short()` (verify core.Hash.Short()'s width — spec expects 12 hex chars; if Short() differs, take the first 12 hex chars of String() and note it); `RenderSentinel` = `"<!-- qompack-contract-probe " + token + " -->"`; `ScanTranscriptTail(path, token, tailBytes)` seek-to-tail + bytes.Contains; missing/unreadable file → (false, err) treated by callers as "no observation yet".
- Test rows in task-4-spec.md that DRIVE THE DAEMON (`TestDeclaredProducerSetMatchesArchitecture`, the daemon-route half of `TestMarkerIsWrittenByFlushAndCheckpointOnly`) belong to the NEXT task — implement here only their contract-side halves (e.g. a source-grep or unit test that WriteMarker is the only marker writer in internal/contract). Everything else in the table lands here, including `TestFreshBuildReportsModeFull` (no producers declared → all nine not-yet-implemented → ModeFull) and `TestAdditionalContextGatedUntilRehydrator` (counting-closure proof the check body never runs when undeclared).
- Fixtures `history_degraded.json` and `transcript_with_sentinel.jsonl`: place them following the repo's existing golden-fixture convention for the contract package (look at how golden_test.go locates its files).
- Import discipline: contract imports foundation + hookio + store only (paths/config/core/logging/obs are foundation). No net, no os/exec.

## Process

- TDD: failing tests first (RED evidence in report), then implement.
- Before committing: `go test ./internal/contract/... -race -count=2`, `go test ./test/guards/...`, `go test ./internal/ipc/... ./internal/daemon/...` (you touched nothing there — cheap sanity), devtool fmt/lint/vet, full `go test ./...` once, `GOOS=linux go build ./...`.
- Exactly ONE commit, exact message below, no attribution trailers:

```
feat(contract): G9.3 assertions, fail-loud degradation, two-clean-run restore

Every assertion is a real observation rather than a version check, and an assertion
whose producer is absent from the build reports SevInfo/not-yet-implemented so a
wave-1 build cannot degrade itself into passivity and silently disable the paths it
is meant to be testing.

Refs: SP-05, G9.3, §9, §12
```

## Report

Full report → `.superpowers/sdd/V2-SP-05-daemon-ipc-and-hot-path/task-4-report.md` (TDD evidence, files changed, adapted-shipped-tests list with reasons, self-review, concerns). Reply with ONLY: Status, commit SHA+subject, one-line test summary, concerns, report path.
