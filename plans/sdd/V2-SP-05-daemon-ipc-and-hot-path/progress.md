# SDD ledger — plan: plans/V2-SP-05-daemon-ipc-and-hot-path.md

Worktree: C:/Users/Quant/Documents/Programming/Projects/qompack-sp05
Branch: feat/sp05-daemon-ipc-and-hot-path (cut from develop @ 7340536)
Spec authority: plans/00-ARCHITECTURE.md (+ standing amendment branch arch/daemon-options-pointer-receiver: §5.4 Options.Handle has a POINTER receiver) and Qompack.md. The SP-05 plan is the argument; 00-ARCH + shipped SP-01 code win on conflict.

## Task map (plan's 7 commits = 7 tasks, sequential)

- Task 1 = Commit 1: ipc address/state/framing/ops (extends shipped resolve.go/wire.go/addr.go)
- Task 2 = Commit 2: ipc client, spool, server, listen (replaces SP-01 stubs; flip ipctest skips)
- Task 3 = Commit 3: daemon lock, spawn, registry, ingest/WAL, drain, sketchset
- Task 4 = contract producers/history/marker/sentinel/assertions + monitor extensions (the plan's "Commit 5" content and message)
- Task 5 = daemon options/services/idle/budget/handlers/reload/metrics/daemon.go (the plan's "Commit 4" content and message)
  - Ruling (#18): the plan's commit 4 references contract symbols its commit 5 delivers (WriteMarker, LoadHistory, MintSentinel, DeclareProducer) — as ordered it cannot compile. The plan's global rule "every commit compiles and passes tests" outranks its ordering, so commits 4 and 5 swap. Same 7 messages land; `TestDeclaredProducerSetMatchesArchitecture` (drives daemon.DeclareProducers) moves to the daemon task. Cost if wrong: git history ordering deviates from the plan's numbering.
- Task 6 = Commit 6: cli hook clients, daemon cmd, self-test, fault injection, e2e tests
- Task 7 = Commit 7: bench harness, devtool task, CI bench-gate, bench fixtures

## Pre-flight conflict scan (plan text vs repo reality)

| # | Pair / task | Found | Ruling |
|---|---|---|---|
| 1 | Plan ipc names vs shipped internal/ipc | Shipped: `NamedPipe`/`UnixSocket` (iota+1), `MaxLineBytes`, `ErrUnresolvedRoot`, unexported `endpointHash`, `Resolve` without env override/case-fold. Plan: `AddrNamedPipe`, `DefaultMaxLine`, `ErrAddrTooLong`, `ProjectHash12/8`, `QOMPACK_IPC_ADDR`. | **Ruling:** shipped spellings win (`NamedPipe`, `UnixSocket`, `MaxLineBytes`). Add exported `ProjectHash12/ProjectHash8`, `ErrAddrTooLong` sentinel, `QOMPACK_IPC_ADDR` override, and case-fold+Abs normalization (mirrors paths.Key/DefaultFold) as additive changes; SP-05 owns ipc and may update its own resolve tests. Cost if wrong: later-wave plans reference plan spellings and would need mechanical renames. |
| 2 | Plan `ipc.Router` + `daemon.NewOptions`/`Routes`/`ErrOptionsUninitialized` vs arch amendment | Amendment (arch/daemon-options-pointer-receiver) fixes §5.4: pointer-receiver `Options.Handle` into lazy unexported map — shipped code already does this (Handle/Handler/Ops on Options). | **Ruling:** NO `ipc.Router` type. Keep shipped map-based table; panic recovery + unknown-op fallback live in the daemon dispatch path. `NewOptions(projectRoot, cfg)` is added as a convenience seeding Log/Metrics/Clock/Sketches. `ErrOptionsUninitialized` dropped (bare Options literal is safe by construction); plan tests TestHandleOnBareOptionsIsSafe reduces to no-panic+routes-land. Cost if wrong: wave-3 plans that call ipc.NewRouter need updating (they call opts.Handle, which survives). |
| 3 | Plan obs/budgets.go vs shipped internal/obs | SP-01 shipped budgets.go + stateful CheckBudgets COMPLETE: Budget{ID,Hist,Pct,Gated,Limit func(cfg)}, hist names "hook_controlled","l0_ingest","l0_process","hook_wall","checkpoint_finalize","mcp_tool_call" (unexported consts; reachable via Budget.Hist), limits from cfg.Runtime.Budgets.* config keys, `obs.New(clock)`, `Counter.Add`, `Persist(paths.Layout)`. | **Ruling:** SP-05 makes NO changes to internal/obs. Daemon resolves hist names via obs.Budgets()[..].Hist; the extra observed-lower-bound histogram is named in daemon (`hook_controlled_observed`). Plan's four nomagic allowances in obs are obsolete (limits are config keys); exit-criterion "exactly five allowances" is void — the binding rule is: nomagic pass clean. Cost if wrong: metric names in status payload differ from plan's dotted names; SP-14 renders whatever the snapshot carries. |
| 4 | Plan contract.History struct vs shipped `History` interface in Env | Shipped Env.History is an interface {Saw, LastSeen, Record, Sessions}; contracttest.RunHistorySuite pins it; NO implementation exists anywhere yet. Plan wants a rich persisted struct + LoadHistory/SaveHistory at state/contract.json — which collides with the monitor's own persisted state at that exact path. | **Ruling:** keep the interface. Task 5 builds `contract.SessionHistory` (exported concrete struct): plan's History fields (Go field `SessionCount int` with json:"sessions" to dodge the Sessions() method collision) PLUS `Seen map[ID]core.UnixMilli` backing Saw/LastSeen/Record; implements the History interface; satisfies RunHistorySuite. `LoadHistory`/`SaveHistory` persist it to `state/history.json` — NOT state/contract.json, which stays the monitor's own file (two schemas on one path would corrupt each other; plan text deviates here by necessity). Cost if wrong: one extra state file; renameable later. |
| 5 | Plan RunAll semantics vs shipped monitor.go | Shipped RunAll: no runtime.mode short-circuit, no per-assertion recover, Degrade re-logs on EVERY critical run (deliberate per comment). Plan: mode off/passive/full short-circuit/forcing, panic recovery → SevWarn "assertion panicked", degrade re-logs only on reason change. | **Ruling:** extend shipped monitor with runtime.mode handling + per-assertion recovery (plan wins; both are capability adds). Degrade loudness: keep SP-01's every-run re-log (its comment is a deliberate, tested decision; plan's idempotence test is adapted: same-reason re-degrade may re-log). Cost if wrong: noisier LOUD.log in stuck-degraded sessions; trivially revisitable. |
| 6 | Plan declared severities vs shipped StandardAssertions | Shipped declared: CPreCompactTiming=SevWarn, CMCPRegistered=SevInfo. Plan: timing SevCritical-on-timeout (observation-dependent), MCP SevWarn. RunAll reads Result.Severity (observed), not declared. | **Ruling:** keep shipped declared severities; the real checks return observed severity per plan's table (timeout→SevCritical result etc.). Golden pins to be respected per inventory. |
| 7 | Plan paths API vs shipped | WriteAtomic(p,b,perm) 3-arg; AppendOnly returns io.WriteCloser; CreateNew(p,b) writes content; paths.Resolve(getenv,cwd) already implements §3.3 root resolution; paths.Layout/Of/EnsureLayout exist; paths.Long for Windows. | **Ruling:** briefs carry shipped signatures; cli uses paths.Resolve, not a reinvented walker. |
| 8 | Plan cli skeleton vs shipped cli framework | Shipped: Cmd table w/ Env injection, Dispatch enforces hook-exit-0 + runGuarded recovery; hooks are observation no-ops using paths.Resolve + config load. | **Ruling:** SP-05 rewrites hook bodies INSIDE the shipped Cmd/Env framework (state.bin-first flow per plan); keeps env injection (env.Getenv/env.Stdin, never os.*); recoverToZero maps onto shipped runGuarded/recover.go where possible. |
| 9 | daemon.SketchSet shipped shape | Shipped: struct{Tried,Touch,Explore,Top} no mutex, no Load/Save. Plan: adds mu, NewSketchSet(cfg), Load/Save/Read/Write; no Top. | **Ruling:** keep Top field; add plan's members additively. tried.bloom never written by Save (both agree). |
| 10 | Task ordering: wire.go Response already imports contract | ipc→contract dependency already shipped; contract pkg compiles fully; Tasks 1–4 can precede Task 5 without missing symbols (ParseMode/MayAct additions needed by daemon land in Task 4? NO —) | **Ruling:** `ParseMode` (exported), `Mode.MayAct`, `Mode.MayRecord` are added to contract/mode.go in **Task 1** (tiny, additive, needed by ipc/state.go and daemon). Ledgered so Task 5's implementer knows they exist. |
| 11 | Plan's 32-byte state.bin `Mode` field | ipc.State carries contract.Mode; ReadState fallback = StateFromConfig(config.Defaults()) — needs runtime.mode parse → uses contract.ParseMode (see #10). | consistent. |
| 12 | Bench harness / devtool / CI | devtool already declares `bench-hotpath` as a no-op placeholder printing "harness not present (owned by SP-05)"; ci.yml bench-gate exists with `continue-on-error: true` + a comment that SP-05 removes it. | **Ruling:** Task 7 implements the devtool task body, adds test/bench/hotpath, removes bench-gate's continue-on-error (leaves replay-gate's alone — SP-02 owns it). |
| 13 | `qompack version` | Already ships in cli/commands.go All(), prints core.Version. | No work needed; bench floor measurement uses it as-is. |
| 16 | ipctest factories | Four Run*Suite(t, name, factory): Client / SpoolWriter / Server / Transport{Client,Addr}(+Handler). Skip literal "behaviour: implementation is a stub (Rule W-1)" via core.IsNotImplemented probes. No Server constructor exists yet — SP-05's NewServer defines the shape and wires all four factories. | Task 2 wires them; zero remaining skips per Rule W-1. |
| 17 | OWNERS.tsv | ipc, daemon, contract assigned to SP-05; coverage floor 75%; stub probes Send/Run/RunAll. | consistent with plan exit criteria. |
| 19 | Plan's observe_tool.ndjson golden (4-key Event) vs shipped hookio.Event (no omitempty → 12 keys) | hookio is SP-01-owned and out of SP-05 scope; other wave-1 branches build against it concurrently. | **Ruling:** do NOT touch hookio. The golden fixture is CREATED by Task 1 and pins the real bytes EncodeRequest produces over the shipped Event (all fields, compact, no HTML escaping, one trailing \n). The plan's 4-key example line is illustrative, not normative; the normative properties are compact JSON + short Request keys + single newline + byte-stability. Cost if wrong: ~150 dead bytes per hot-path line until some later subplan adds omitempty and regenerates the golden. |
| 14 | Commit-plan checklists referencing obsolete items | Commit 1 fixture state_degraded.bin unaffected; Commit 4 checklist "Add internal/obs/budgets.go; delete stub CheckBudgets" is void per ruling #3. | Ruling recorded. |
| 15 | Windows-only local host | POSIX branches (unix sockets, chmod modes, Setsid) compile-checked via GOOS=linux/darwin builds; behavior verified in CI only. Plan already anticipates this. | noted. |

| 20 | Plan's exact commit subjects vs repo commit-msg hook | devtool check-commit-msg caps subject free text at 64 chars; several plan subjects exceed it. | **Ruling:** minimal meaning-preserving trims, pre-applied to briefs 2/4/5/6 (Task 1's implementer already trimmed its own). Never bypass the hook. |

## Global constraints carried into every brief

- Never modify Qompack.md; never write outside .qompack/ and ~/.qompack/ at runtime.
- Import discipline (devtool importrules + test/guards): ipc → foundation+hookio+contract only; contract → foundation+hookio+store; daemon/cli composition roots; nothing imports daemon/cli. net only in internal/ipc, "unix" only. os/exec only in daemon/cli/tools.
- nomagic: deadlines/budgets/window sizes come from config.Config or named constants with //nomagic:allow + §-reference.
- Every time-taking component takes core.Clock; time.Sleep banned outside test/bench.

## Task log

Task 1: implemented (commit 4dc422b5, DONE_WITH_CONCERNS: subject trimmed for 64-char hook; WriteState Gosched retry for Windows rename contention; frozen-fixture count bump in testutil/guards). Review dispatched.
Task 1: minor (deferred): WriteState permission-retry not gated to GOOS==windows (state.go); LineReader doc overclaims memory bound for maxLine < bufio default (frame.go); decodeState doesn't range-check the mode byte (fails open to MayAct for unknown values); Op.Valid() re-sorts KnownOps per call — precompute before any per-frame use (op.go).
Task 1: ⚠️ resolutions: commit body/Refs/no-trailers verified by controller via git show (clean); fmt/lint/vet claims re-covered by each later task's pre-commit runs.
Task 1: complete (commits 7340536..4dc422b5, review clean — Approved, 0 Critical/Important)
- Hook subcommands ALWAYS exit 0 (§2.3); self-test is the only non-zero-permitted subcommand.
- TDD: tests written first per commit, must fail for the stated reason before implementation.
- Commit messages: Conventional Commits + Refs footer, NO Co-Authored-By/attribution trailers. Exactly 7 commits total.
- gofumpt, golangci-lint, go vet clean; `go build ./...` under GOOS=windows, linux, darwin.

Task 2: implementer DONE (commit 13c448f9, one commit, subject exact). Concerns flagged: (a) ACL cross-principal test needs CI second-account job (SDDL shape asserted unconditionally); (b) listen() gained a log param for the Windows SDDL-fallback Loud line — documented deviation, accepted pending review; (c) POSIX listen already-in-use returns wrapped error not daemon.ErrLockHeld (ipc cannot import daemon — correct per import discipline). Review package review-task2.diff (96KB, corpora excluded) dispatched to task reviewer (opus).

Ruling #21: POSIX listen "address already in use" gets an ipc-local exported sentinel `ErrAddrInUse` (errors.Is-able; the daemon translates to its own ErrLockHeld semantics in Task 5) — the brief barred importing daemon but a bare wrapped error leaves callers string-matching — costs: one more exported symbol if wrong.
Task 2: review verdict Needs fixes (1 Critical: spool double-newline; 7 Important: l0_spooled-on-failed-append, externalize clobbers Raw, no read deadline + Close leaves conns open (shutdown hang), post-wait os.Remove can unlink successor socket, missing ErrAddrInUse sentinel, rapid.checks flag leak halves frame_test property runs, ipc_decode_error untested; 13 Minor). Fix round 1: resume implementer a3ab2aead84d202c2, amend into the single task commit (7-commit plan constraint).

Task 2 fix round 1: all 8 C/I findings fixed and verified by scoped re-review (commit amended to e4dbd3b9). Reviewer adjudications: M-4 reverted correctly — paths.IsProtected omits spool/ (PRE-EXISTING internal/paths scope gap; carry to final review / later subplan); connIdleTimeout=10min unconfigurable is acceptable at this layer (re-evaluate when MCP-over-daemon lands). Deferred minors now: task-2 M-1,M-2,M-3,M-5,M-8,M-12,M-13 + N-1,N-2 (N-1/N-2 sent to fix round 2 as trivial). NEW Important N-3: POSIX-only test file never executed — TestStaleUnixSocketReclaimed self-defeating (unlink-on-close). Fix round 2 dispatched (same implementer): SetUnlinkOnClose fix + WSL/docker execution attempt, else desk-verify + CI reliance note.

Task 2: complete (commit dd2f5e22 after 2 fix rounds, review Approved). Deferred to final review: task-2 minors M-1,M-2,M-3,M-5,M-8,M-12,M-13 + N-2a (wg.Add outside connsMu critical section — fold into trackConn with closed re-check) + N-2b (live-conn close test bound should be serverCloseWait/2 — the two waits run concurrently, 3s bound cannot catch a dropped closeTrackedConns) + pre-existing paths.IsProtected spool-tier gap + unconfigurable connIdleTimeout note. MERGE CONDITION: ubuntu+macos CI legs must be observed green before merge — server_unix_test.go/listen_unix.go have never executed anywhere.
Task 3: dispatching implementer, BASE=dd2f5e223cae9f4755c40c375d0694b994d6e79c.

Task 3: implementer DONE_WITH_CONCERNS (commit a73a2384, subject "feat(daemon): singleton lock, session registry, WAL ingest queue, and drain"). Concerns: (a) lock liveness probe assumes future daemon answers OpAdminPing OK:true — Task 5 must satisfy; (b) drainer may delete its own live client-<pid>.ndjson spill target once ingest+drain run concurrently. Both handed to the Task 3 reviewer (opus) for adjudication; review package review-task3.diff (94KB).

Task 3 review: Needs fixes (0 Critical, 5 Important: I-1 no blob resolution on live ingest path + seenSet records pre-resolution; I-2 heartbeat-after-CreateNew stale-takeover race can seat two daemons on Windows; I-3 EnsureRunning requires Response.OK causing spawn storm + 1500ms stall on non-OK ping; I-4 drain.json persisted end-of-Drain not per-file-EOF; I-5 redundant ring-full spill with unpinned byte-identity assumption; 12 Minor).
Ruling #22: liveness = successful dial. Export `ipc.Probe(a Addr, timeout) bool` (dial + close, no request); EnsureRunning and lock liveness use it; admin.ping round trips only where response content matters. Task 5 additionally pins admin.ping→OK:true with a guard test. — cheaper and assumption-free vs pinning alone — costs an extra exported symbol.
Ruling #23: accept reviewer's adjudication of implementer concern (b): the ring-full spill (clientSpool/spill) is redundant by construction (WAL-first ordering makes every spilled line already durable); DELETE it, keep l0_ring_full counter. Also fix undeclared adaptation (a): B-B l0_ingest timing must wrap the full Accept path (all three steps), not only the WAL append. Ensure's cfg param: accepted adaptation.
Task 3 fix round 1: resume implementer a9c3169b (amend into single commit).

Task 3: complete (commit eeca0651 after 1 fix round, review Approved; all 12 minors fixed). Deferred to final review: once-observed unnamed transient in internal/ipc/ipctest under heavy load (not reproducible in 3 reviewer runs; likely 10s suiteWait deadline guards) — final reviewer should eyeball those guards; ring-full test 500ms-comment overstatement and the spec's never-implementable "<1ms on FakeClock" note recorded. Process note for future dispatches: implementers must report transients with test name + failure text.
Task 4: dispatching implementer, BASE=eeca0651c8f5c90eb36c2a8db95673bd316a1feb.

Task 4: implementer DONE_WITH_CONCERNS (commit 506baa01). Concerns handed to reviewer: (a) session_start.fires resets StartsWithoutMarker on hit — untested inference; (b) SessionHistory sub-field shapes implementer-designed; (c) testutil frozen-fixture count bumped 26→28 (two new frozen fixtures — same mechanism as Task 1). Review package review-task4.diff (92KB) dispatched (opus).

Task 4 review: Needs fixes (0 Critical, 6 Important: I1 StartsWithoutMarker per-run not per-session; I2 empty ProjectRoot violates nil-tolerance in marker check; I3 dropped >=24-char probe-phrase rule makes custom-instr assertion unfailable; I4 mode "off" leaves stale force, Mode() stays actionable; I5 SessionHistory unsynchronized with no documented ownership; I6 report's "adjustable sub-shapes" advice false — frozen fixture pins wire shape; 10 Minor). Concern adjudications: (a) consecutive-absence reading accepted, must be pinned by test; (b) SentinelState shape accepted; (c) fixture bump verified sanctioned/additive-only.
Ruling #24: RunAll("off") forces ModeOff in-memory (same force mechanism as passive/full) so Mode()/MayAct reflect operator-off; auto clears it. — closes the I4 trap before Task 5 consumes Mode(). Costs: none foreseen; force stays in-memory by design.
Ruling #25: SessionHistory's concurrency contract is external serialization — document on the type that the daemon owns a single mutex (Task 5 brief already mandates it); no internal locking added in contract. — keeps the type a plain data record.
Task 4 fix round 1: resume implementer aad05f71.

Task 4 fix round 1: DONE (amended to 35a1901b; minors M1-M5,M8-M10 fixed; M6/M7-behavioral left).
Ruling #26: M6 — the deferred contract observability lands in Task 5: the daemon increments underscore-idiom counters (contract_fail_<id-ish> per failing assertion and a contract_mode gauge-style counter on mode change) after RunAll; contract package itself stays metrics-free. M7 behavioral half (LOUD restore line during force) accepted as documented wrinkle — carry to final review. Task 5 brief must also carry I5/I6 corrections: SessionHistory single-owner mutex + frozen wire shape (changes = new-version migration).

Task 4 re-review: I1-I6 all fixed; NEW-1 Important (LastSessionID undocumented ownership trap — daemon writing it would silently disable session_start.fires) + 3 minors → fix round 2 dispatched. TASK 5 BRIEF ADDITIONS (binding): daemon must NEVER write SessionHistory.LastSessionID (owned by session_start.fires); SessionHistory wire shape frozen (changes = new-version migration); single daemon-owned mutex serializes all history access; ruling #26 counters (contract_fail/contract_mode after RunAll) land in Task 5.

Task 4: complete (commit 29585ce1 after 2 fix rounds, review Approved). Deferred to final review: NEW-5 minor (test comment credits wrong variant — fold into any later touch), M7 behavioral half (LOUD restore line during force). Reviewer-recorded residual risk: LastSessionID protection is documentary only — Task 5 brief carries the hard constraint.
Task 5: dispatching implementer, BASE=29585ce1dcd8237de49c5bf044d34ce62339904a.

Task 5: implementer DONE (commit 115ba19d). Concerns: (1) PRE-EXISTING flaky -race failure in ipc server Close/waitForConns (reproducible via Task-3 lock/spawn tests on unmodified code — almost certainly Task 3's "unnamed ipctest transient"; branch-owned Task 2 code, MUST be fixed before merge — reviewer asked to diagnose); (2) admin.shutdown runCancel bug found+fixed mid-task with test; (3) nine nil-tolerant Services seams inferred from handlers table. Review package review-task5.diff dispatched (opus).

Task 5 review: Needs fixes (1 Critical: drained flush/admin.drain line re-enters Daemon.Drain inside drainer.mu — permanent self-deadlock, startup drain can wedge daemon pre-Serve; 8 Important: sentinel Observed/Chances wiped each session start vs "never resets" invariant; CheckBudgets per-sample not per-window (~512x inflation); missing state.bin rewrite on reload (dropped requirement); historyMu held across RunAll + unbounded seam calls; unvalidated req.TS feeds B-A histograms + degrade; Run ctx.Done path skips Stop entirely; breachDetector.Reset never called; writeLatencyJSON vs obs.Registry.Persist shape conflict on same file (dropped ruling-#20 half); 12 Minor).
ipc race DIAGNOSED: N-2a manifesting exactly — Close→wg.Wait races Serve's wg.Add in the window after Accept returned a conn but before Add; listener close only unblocks a PENDING Accept; same window leaks a conn past closeTrackedConns. Reproduced 2/2 via daemon lock/spawn tests -count=10.
Ruling #27: N-2a promoted deferred-minor → fix-now. Fix authored by the Task 2 implementer agent IN THE TASK 2 COMMIT via history surgery (branch unpushed, sanctioned): detach at dd2f5e22, fix server.go (wg.Add inside connsMu behind a closing flag set by Close under the same lock; conns accepted after closing are refused+closed), amend, cherry-pick eeca0651/29585ce1/115ba19d, re-point branch. Fold in deferred N-2b (bound → serverCloseWait/2) and the N-2a comment fix while there. THEN Task 5 fix round runs on the new HEAD. Costs if wrong: cherry-pick conflicts — none expected (no later commit touches server.go).

Ruling #27 executed: N-2a + N-2b fixed in the Task 2 commit; history replayed with zero conflicts. NEW SHA MAP: C2'=4e91fcac (ipc), C3'=d0e9f006 (daemon blocks), C4'=98c4fef7 (contract), C5'=04f3c81f (daemon composition). Reproducer 10/10 clean. NEW DEFERRED (final review + follow-up ticket): go-winio v0.6.2 win32PipeListener.Close can hang 20s+ racing a fresh Accept (observed 0-84/100 iterations); our Close now bounded via closeListenerBounded, but Serve's goroutine can linger past serverCloseWait pending go-winio Accept unblocking — Windows-only, needs newer go-winio or cancellable-Accept redesign; NOT fixable at server.go level.
Task 5 fix round 1: resume implementer a1dedd92 on HEAD=04f3c81f.

Task 5: complete (commit ef50515f after 1 fix round on replayed history, review Approved). Deferred to final review: hotPathSampleMaxAge discards (not clamps) genuinely extreme live samples — the case the fallback exists for; ctx.Done propagates Stop error while idle-exit arm ignores it; M-8 (SP-13 replaces route), M-11 (architectural "consider"), M-9 Loud-line half. ipc reproducer clean 3/3 — N-2a closed at daemon level.
Task 6: dispatching implementer, BASE=ef50515fb95c7d050554119006d0b73cf3e1d45f.

Task 6: implementer DONE (commit 8c84cf9; the earlier user-stop was transient — agent resumed and finished). 66/66 fault matrix green. Concerns to reviewer: (1) fixed pre-existing repo-checkout write hazard in dispatch_test.go fault case; (2) broke daemon->testutil->cli import cycle with daemon-local fakeClock; (3) added golang.org/x/sys/windows to devtool bindeps allow-list; (4) updated V1-checkpoint e2e/testutil tests tied to removed wave-0 hook-log behavior — reviewer must verify no frozen fixture/guard broken; (5) selfPath() gates on testing.Testing() to prevent spawn storms in test binaries — judgment call to adjudicate. Review package review-task6.diff dispatched (opus).

Task 6 review: Needs fixes (2 Critical: self-test permanently pollutes immutable checkpoint tier; hot path reads state.bin twice with every drop invisible (nil registry + Nop logger — no l0_dropped, no Loud); 9 Important incl. deleted TestE2E_AllSixHooksExitZero (named by five downstream VERIFY gates — MUST be restored under exact name), IT-1 payload-leak + IT-2 bootstrap-clamp assertions dropped, daemon-down fault site never disables the address, spool-submode test asserts a counter that cannot move, metrics nil where spec names SP-01 registry ctor; 12 Minor). Adjudications: (a) drive-by legit (stale sub-test name); (b) fakeClock legit; (c) x/sys/windows legit (M-14 prefix breadth); (d) mostly legit except the deletions; (e) testing.Testing() diagnosis right placement wrong — links testing+runtime/trace into shipped binary.
Ruling #28: selfPath becomes an injectable var (test override), removing the testing-package link from the shipped binary; TestE2E_AllSixHooksExitZero restored verbatim-named; weakened assertions restored with correct counters.
Task 6 fix round 1: resume implementer ac50a02e.

Task 6 fix round 1: DONE (amended to bdc9585c; 2C+9I fixed, 12/12 minors + M-1/M-8 folded). NEW mid-verification fix flagged for scrutiny: 250ms connect-deadline floor for reply ops only (session-start/checkpoint/flush) — the removed double state.bin read had been masking ConnectDeadlineMs=5ms being too tight for the first post-EnsureRunning dial. DEFERRED (final review + future-subplan note): daemon drain cadence — spool drain only at start/stop/idle-tick, so a missed dial's spooled line can sit unrecovered on a continuously-active project.

Task 6 re-review: all round-1 fixes verified; connect-deadline mitigation ACCEPTED at cli layer (right layer, fits deadline table; observe.tool/stop untouched). NEW DEFERRED (final review): EnsureRunning readiness contract weakness — ipc.Probe consumes the accept slot it uses as proof, so probe-success never implied next-dial-lands-in-5ms (ipc/daemon-layer fix candidate for a later subplan). Fix round 2 dispatched: N-1 stale ClientOptions.State doc, N-2 connect-floor predicate must exclude HotPath ops (observe.prompt leaked in), N-3/N-4/N-5 minors.

Task 6: complete (commit 6fc27a3b after 2 fix rounds, review Approved). Deferred to final review: N-6 minor (fault_noinject.go stale one-file claim + phantom constant clause — 2-line doc cleanup).
Task 7: dispatching implementer, BASE=6fc27a3b082604313074fd04b32f510249687053.

Task 7: implementer DONE_WITH_CONCERNS (commit ab088f4f). Local n=2000: B-A p99=60.4ms FAIL (traced to sandbox spawn jitter — bare cmd.exe spawn ~20-24ms p50; daemon hook_controlled_observed p99=2ms; budget NOT relaxed; CI adjudicates), B-B 0.64ms PASS, B-E 136.7ms PASS. Concerns: warm-up uses Client.Send per-request (net-import guard forbids a held conn outside internal/ipc — harder constraint than spec wording); bench-gate required-check on develop/main is a GitHub branch-protection setting, NOT a repo file — flag to user at finish. Review package review-task7.diff dispatched (opus).

Task 7 review: Needs fixes (0 Critical, 3 Important, 7 Minor). Reviewer's independent finding (verified by own runs): the SPEC's step-6 estimator (wall-clock spawn minus constant floor_p50) is ~unbiased at median but contaminated at p99 by spawn-time dispersion the subtraction cannot remove; B-A per obs/budgets.go is "main() entry to exit" (EXCLUDES process creation); bench B-D p99 < bare-version-floor p99 proves the tail is host dispersion, not product. Product side healthy: daemon observed p99<=2.048ms, B-B passes 3.5x, no budget relaxed, delivery exact.
Ruling #29 (spec amendment, step 6): the B-A GATE consumes the architecture's own TS-anchored estimate — the daemon's estimated hook_controlled histogram (observed + hotPathTailAllowance, exactly what the breach detector consumes), fetched via status — and gates its p99 against 15ms. Wall-clock spawn numbers stay in out.json as INFORMATIONAL diagnostics (not gated) with the floor documented as unremovable-dispersion caveat. Rationale: gates must measure what the budget defines; the spec's method cannot honestly pass on any shared runner and would misattribute host jitter to the product. Costs if wrong: an in-hook regression between ACK and process exit (microseconds of output-write) escapes the gate — covered by hotPathTailAllowance's documented margin.
Escalations recorded for branch finish (user-facing): (1) mark bench-gate required in branch protection only AFTER first green CI on all three platforms; (2) replay-gate's continue-on-error is SP-02-owned — leave it despite V4-VERIFY prose flagging it.
Task 7 fix round 1: resume implementer ab750467.

Task 7: complete (commit b6181b7a after 2 fix rounds, review Approved). Ruling #30 (formalizing R2-1): the brief's binding warm-up ruling ("2000 observe.tool / ~40MB") is AMENDED alongside #29 — warm-up = warmHotTranche(64) hot-path requests + bulk admin.ping (accept-loop warming without polluting the gated histogram); the exact-rank property holds at k=2000/5000, residual small-n bias is conservative (spurious FAIL never spurious PASS). Deferred minors: R2-2 (unconditional "hook-spawn-dominated" note sentence — make proportional/conditional), R2-3 (optional small-n percentile-collapse warning), p999>max snapshot observation (pre-existing, deliberate).
ALL 7 TASKS COMPLETE. Branch develop..HEAD: 4dc422b5, 4e91fcac, d0e9f006, 98c4fef7, ef50515f, 6fc27a3b, b6181b7a (=7 commits, plan-mandated count met).
Dispatching FINAL whole-branch review (most capable model) with the ledger's deferred-minors list.

FINAL REVIEW: Needs fixes (0 Critical, 2 Important FR-1 registry read outside mutex / FR-2 silent observe.prompt WAL-append failure; 5 fix-now minors FR-3 blobRef path traversal guard, FR-4 Serve-failure arm skips Stop, FR-6 DaemonEnabled gate on preSend, N-6 fault_noinject doc, R2-2 proportional note; ~22 accepted/resolved). ALL EXIT CRITERIA PASS (coverage ipc 84.2/daemon 82.0/contract 83.9/cli 81.5; W-1 zero; 7 commits byte-match; trailers clean). Bench gate CONDITIONAL on first 3-platform CI green (pre-merge condition stands).
Fix wave (ONE dispatch, Task 5 implementer): fold daemon items (FR-1..FR-4) into commit 5, cli items (FR-6, N-6) into commit 6, bench item (R2-2) into commit 7, via sequential detach-amend-cherry-pick replay.

FIX WAVE COMPLETE + FINAL REVIEW APPROVED at HEAD 93f71262. Branch: 4dc422b5, 4e91fcac, d0e9f006, 98c4fef7, 3bd35994, d040fc07, 93f71262 (7 commits). All exit criteria PASS. Residual (documented, accepted): FR-6 first-run window (first session-start on a never-daemon'd config-disabled project spawns once — unavoidable under no-config.Load-on-hot-path). Pre-merge conditions: (1) ubuntu+macos CI legs must be observed green (first-ever execution of listen_unix.go/server_unix_test.go); (2) bench-gate green on all 3 platforms BEFORE flipping branch protection; (3) replay-gate continue-on-error is SP-02-owned — leave it. SDD execution complete.

Pre-merge full-suite gate caught: TestAdminShutdownStopsTheDaemon intermittent Windows failure — TempDir RemoveAll races something still writing/holding .qompack/tmp after admin.shutdown's async Stop. Dispatched daemon implementer for root-cause fix folded into commit 5 (replay); menu deferred until suite is green.
